#include <stdio.h>

int main(){
	// conditional statments uwu
	
	// if  statements
	
	// 1 == true
	// 0 == false
	
	// is not 0, it's always true (int) 1, juan bilion, 30, 69 funny
	
	// truthy values = "strings", 'C', 1, ints, True
	// falsy values (sussy) = "", 0, False
	
	if(""){
		printf("i am wrong");
	}
	
	
	int did_i_eat_dinner = 0, do_i_want_desert = 0, am_i_full = 0;
	
	if(did_i_eat_dinner){
		if(do_i_want_desert){
			printf("cake");
		} 
		printf("shit");
	} else if(am_i_full){
		printf("starve/maybe take a shit");
	} else {
		printf("unli wings");
	}
	
	
	did_i_eat_dinner = 1;
	// did_i_eat_dinner = did i eat dinner
	// !did_i_eat_dinner = did i not eat dinner
	
	// determining if we should wake up early or sleep in
	// when should we wake up early = when it is a weekday
	// when should we sleep in = when der is a weekend or a holiday
	
	int is_weekday = 1, is_weekend = 0, is_holiday = 1;
	
	int sky_blue = 1;
	
	// comparing numbers/values
	// == - is equal to
	// != - not equal to
	// < - less than
	// <= - less than or equal to
	// > - greater than
	// >= - greater than or equal to
	
	// && = and
	// || = or
	// ! = not
	
	// out the window^^^ when using case-switch statements
	
	// !0 = 1
	// !1 (or any other number, or truthy value:>) = 0
	
	if(is_weekday && is_holiday && 1 == 1 && sky_blue){ // evaluates to true/ 1
		printf("go back to sleep");
	}
	
	if(is_holiday || is_weekend || 1 == 0){
		printf("go back to sleep");
	}
	
	
	// case switch statements
	// what it is
	// when to use
	
	
	
	// ternary operators
	// what it is
	// when to use
	return 0;
}
